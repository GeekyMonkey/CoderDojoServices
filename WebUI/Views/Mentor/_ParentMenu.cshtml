@model Adult
@{
    string activePage = ViewBag.MenuActive;
    string activeClass = "ui-btn-active ui-state-persist";
    string inactiveClass = "";
    string adultId = Model.Id.ToString("N");
}

<div data-role="navbar">
    <ul>
        <li><a href="/Mentor/Adult/@adultId" class="@(activePage == "Details" ? activeClass : inactiveClass)">Details</a></li>
        <li><a href="/Mentor/ParentKids/@adultId" class="@(activePage == "Kids" ? activeClass : inactiveClass)">Kids</a></li>
    </ul>
</div>
